 This is a conversation to create specifications for an implementation of AlphaZero. See `.local/refs/*` for reference materials including papers and links to existing implementations. We will not be writing any code: instead the goal is to create specification markdown documents under `specs/`. Future AIs will use the specs to develop an implementation plan and ultimately build the program. I want you to help me fully define the technical specifications of this program.

We are developing a fast implementation of Google DeepMind's AlphaZero that should be able to play Go and Chess at a minimum. The target hardware is an Nvidia DGX Spark running Ubuntu 24.04. CPU and GPU operations must be optimized. The alphazero model will be trained on a dgx spark. Help me make architecture decisions and spec out the program.
