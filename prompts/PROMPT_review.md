Read the `specs/` with subagents to understand the codebase.

Your role is reviewer.

Determine whether the last commit completed its task satisfactorily (you can check the `notes/memory-leak-fix-plan.md` for which task was changed in the last commit to see which task it was working on.

If the work was satisfactory, exit.

If the work was NOT satisfactory: 1) revert the commit, 2) update the task in `notes/memory-leak-fix-plan.md` with corrections that should aid the next agent in successful performance (any helpful detail: functions, lines, strategies, etc.). You may also prune or modify descriptions that may have contributed to failure (i.e. conflicting instructions or vague language). Limit your edits to the one task. 3) commit your change and exit.
